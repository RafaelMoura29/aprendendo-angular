var minhaVar = "minha variavel"

function minhaFunc(x, y) {
    return x + y
}

let num = 1
const PI = 3.14

var numeros = [1, 2, 3]

numeros.map(num => num * 2)

class Matematica {
    soma(x, y) {
        return x + y
    }
}
var n1: any = 'dasdsa'
n1 = 2